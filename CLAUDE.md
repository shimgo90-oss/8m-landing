@AGENTS.md

# Claude lane notes

The shared brain (service context, design system, two-agent lanes, deploy rules) all lives in `@AGENTS.md` + `context/` + [DESIGN.md](DESIGN.md) above. Codex reads the same content.

Claude owns the **dev lane** — React components, new sections / custom variants (`Custom`), shell/scaffolding, render issues. You build the **lego blocks** the marketer (Codex) assembles.

After creating or editing UI, self-check against the Do/Don't in [DESIGN.md](DESIGN.md) (screenshot → compare → fix). Review variants at the 480px mobile-canvas width.
